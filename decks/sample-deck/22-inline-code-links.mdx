---
type: content
notes: |
  Demo of inline code and links.
  A pattern frequently used in technical descriptions.
---

# Inline Code & Links

<div style={{ display: "flex", flexDirection: "column", justifyContent: "center", height: "100%", gap: "2rem" }}>

Embed code and links within text.

<div style={{ background: "rgba(0, 0, 0, 0.04)", borderRadius: "1rem", padding: "2.5rem", display: "flex", flexDirection: "column", gap: "1.8rem" }}>

<div>
<p style={{ fontSize: "2rem", fontWeight: 600, marginBottom: "0.5rem" }}>Installation</p>

Install with `npm install nipry` and start the dev server with `npx nipry dev`.
</div>

<div>
<p style={{ fontSize: "2rem", fontWeight: 600, marginBottom: "0.5rem" }}>Configuration</p>

Configuration is managed in `deck.config.ts`, and each slide is authored as an `.mdx` file.
</div>

<div>
<p style={{ fontSize: "2rem", fontWeight: 600, marginBottom: "0.5rem" }}>Type Safety</p>

Use the `defineConfig()` helper for type-safe configuration.
</div>

</div>

---

See the [official documentation](https://nipry.dev/docs) for more details.

</div>
