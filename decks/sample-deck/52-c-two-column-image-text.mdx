---
type: content
notes: |
  Two-column layout with a small image at the top of each column followed by text.
---

# Platform Strategy

<FigureShowcase
  variant="column-image-text"
  items={[
    {
      title: "Current State",
      description: "The existing platform serves 50k daily active users across three regions. Core infrastructure runs on a monolithic architecture deployed to a single cloud provider. Development velocity has slowed as the codebase grows, with deploy cycles averaging 2 weeks. The test suite takes 45 minutes to complete, blocking rapid iteration."
    },
    {
      title: "Target State",
      description: "Migrate to a microservices architecture with multi-region deployment. Enable independent service deployments with CI/CD pipelines targeting sub-hour release cycles. Scale to support 500k DAU with auto-scaling and edge caching across 12 regions. Implement distributed tracing and centralized logging."
    }
  ]}
/>
